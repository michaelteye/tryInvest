kind: PersistentVolume
apiVersion: v1
metadata:
  name: bezo-postgres-pv-volume
  labels:
    type: local
    app: bezo-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bezo-postgres-pv-claim
  namespace: bezo-dev
  labels:
    app: bezo-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
