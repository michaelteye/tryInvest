export enum TRANSACTION_STATUS {
  pending = 'pending',
  success = 'success',
  failed = 'failed',
  cancelled = 'cancelled',
  refunded = 'refunded',
  disputed = 'disputed',
  chargeback = 'chargeback',
  reversed = 'reversed',
  expired = 'expired',
}
